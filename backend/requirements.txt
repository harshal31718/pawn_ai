fastapi
uvicorn[standard]
pydantic
httpx
httpx2
pytest
pytest-asyncio
pytest-xdist  # parallel test runs: pytest -n auto (full-suite gate)
pdfplumber
python-multipart
numpy
# Upper-bounded 2026-07-23: langgraph 0.6.x broke adispatch_custom_event call
# sites (RuntimeError: "Unable to dispatch an adhoc event without a parent run
# id") when a fresh install landed on that range; 1.2.x is the known-good
# floor verified against this codebase. Re-evaluate the ceiling deliberately
# before bumping, not via an unbounded `>=` pulling a future major silently.
langgraph>=1.2.0,<2.0.0
langchain-core
langgraph-checkpoint-sqlite>=3.0.1,<4.0.0
# Multi-user / auth additions
psycopg[binary]>=3.1
pgvector>=0.3.0
cryptography
google-auth-oauthlib
google-api-python-client
google-auth-httplib2
PyJWT
# Chat agent refinement (Phase A / A.3): fetch_url's readable-text extraction.
trafilatura
